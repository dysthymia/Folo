# 2026-09-16

## 盘点未提交改动（分支 codex/web-actions-ai-v3）

未提交改动整体只服务于一个功能：**Folo Web Actions AI v3.1 的本地「信息工作台」**。

- 新增本地后台 `apps/information-service`（全部未跟踪）：来源分页扫描、单篇/Story 模型处理、Codex CLI + 千问桥、规则草稿/发布 API、反馈、诊断、Notion 导出、评估工具。
- 新增共享包 `packages/internal/information-core`：规则、预设、Actions 规则迁移，纯函数 + 测试。
- 新增前端页面 `/information`（`information-main.tsx`、`pages/information.tsx`、`styles/information.css`、`vite.information.config.ts`、`modules/information/*`）。
- Actions 编辑器新增 `processing_service` 作用域，共 12 个新 `processing-*.tsx/ts` 文件。
- 本地模型接入：`modules/ai-chat/local-provider.ts`，当 hostname 为 `local.folo.is` 时聊天改走本地 `/information/api/chat`，用一次性 token，并隔离官方套餐配额与模型选择。
- 附带修复：`packages/internal/database/src/services/entry.ts` 的 `inArray` 删除改为 500 条分批（避开 SQLite 变量上限）。
- i18n：`locales/app/{en,ja,zh-CN}.json` 各新增 486 条文案。
- 验收台账：`docs/information-v3.1-progress.md`、`docs/information-runtime-status.json`、`docs/information-p0-verification.md`。

未跟踪目录里 `.pnpm-store/` 属本地缓存，不应提交；`.tsconfig.processing-check.json` 是定向 typecheck 用的临时 tsconfig。

## 拆分为 9 个语义化 commit

依赖顺序：`chore(database)` → `chore(i18n)` → `feat(information-core)` → `feat(information-service)` → `feat(ai-chat)` → `feat(actions)` → `feat(information web)` → `chore(scripts)` → `docs(information)`。提交号 c726778d8 … ab7901c0c，基线 91869135a。

关键约束（下次拆这个仓库的 commit 要复用）：

- i18n 必须先落地：`apps/desktop/layer/renderer/src/@types/default-resource.ts` 以 `locales/app/en.json` 作为 `t()` 的类型来源，key 未提交会导致引用它的代码 typecheck 失败。
- `modules/information` 依赖 `modules/action/processing-condition-editor` 导出的 `processingButtonClass`，所以 Actions 分组必须排在信息页之前。
- `pnpm-lock.yaml` 按 hunk 拆到 3 个 commit：information-core 的 importer、information-service 的 importer、renderer 对 `@follow/information-core` 的 workspace link。用 `git apply --cached <单个 hunk 补丁>` 实现，三段汇总与原始 diff 完全一致。
- 仓库 pre-commit 是 lint-staged（`eslint --fix` + `prettier --write`），对全量文件生效；本次静默格式化了 6 个文件（纯换行/import 规范化，无语义变化）。
- 刻意未提交：`.pnpm-store/`（pnpm store 缓存，建议加入 `.gitignore`）、`.workbuddy/memory/`（本地 agent 记忆）。
- 提交前备份：`/tmp/folo-split/worktree-backup-20260916-172747.tar.gz`（1.1MB，含全部涉及路径）。

## 提交消息改写为中文

- 原因：用户要求提交简介改中文。分支 `codex/web-actions-ai-v3` 的 9 个 commit 均未推送（`git branch -r --contains HEAD` 为空），因此改写在安全范围内。
- 方式：用 `git commit-tree` 重建提交对象，只换 message；不跑 rebase，因此 pre-commit 的 lint-staged 不会重跑。保留原 tree、author/committer 姓名邮箱与时间戳。
- 口径：scope 保持英文（对应包名/目录），subject 与正文改中文。
- 校验：`git diff <旧head> <新head>` 为空，且新旧 tree 均为 `cb6e7fac1` → 内容零变化。
- SHA 映射：`ab7901c0c` → `77e2e5d76`（新 HEAD）。旧 head 已记录在 `/tmp/folo-split/old-head.txt`，reflog 亦可回退。
- 脚本：`/tmp/folo-split/reword.py`；该手法已补进 `semantic-commit-split` 技能。

## 推送到远端

- 远端 `origin` = `github.com/dysthymia/Folo.git`（个人 fork，凭据走 osxkeychain）；`upstream` = `RSSNext/Folo.git`（push 已禁用）。
- `origin/yangtao-dev` 实为上游镜像（内容全是上游提交 + 个人 merge），个人功能代码在本仓库的习惯是走独立分支（已有 `origin/codex/fix-typescript-type-error` 等）。因此按此惯例推送为**新远端分支**，未改动 `yangtao-dev`：
  `git push -u origin codex/web-actions-ai-v3`
- 结果：远端 `codex/web-actions-ai-v3` = `77e2e5d76`，与本地 HEAD 完全一致（divergence 0/0），已设置上游跟踪。
- PR 入口：`https://github.com/dysthymia/Folo/pull/new/codex/web-actions-ai-v3`。
