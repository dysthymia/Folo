# 2026-09-26

## 排查：处理规则页的重复规则 / 页面构成 / 标签位置（08:45–09:10）

用户问三个问题，都查到了确切依据，**未做任何改动**（等用户决定）。

**① 两条「Blockchain 同事件综述（v3.2 验收）」的区别 = 只有 `id` 和 `order`。**

- 规则 A `dd6466e1-5543-4224-a0cd-c7bb432beb43` order 0；规则 B `ba98baf6-e71c-48c1-bee4-482ef1ab2cc1` order 1。其余字段（name/enabled/when/actions/version/executionLocation）逐字相同。
- 来源：`rule_set_releases` 里 **v3（2026-09-25T06:51:04Z）只有 1 条，v4（06:51:39Z，晚 35 秒）有 2 条** → 那轮验收发布多复制了一条。
- 后果：**没有功能性后果**。`story-engine.ts:150-151` 按 order 升序，输入被前一条 `claimed` 后，后面的重叠规则在 `candidatesForAction`（`story-engine.ts:268`）被跳过。实测 `stories` 表里 `dd6466e1` 有 8 条综述、`ba98baf6` **0 条**。
- 隐患：两条同名卡片在界面上不显示 id、无法区分；改了一条另一条仍是旧内容；order 0 那条若被停用/删除，order 1 会**静默接管**。

**② 处理规则页（`processing-setting.tsx`）包含的 10 块**：标题描述 / 重新加载 / 状态行（需先验证·草稿rN·未保存·上次发布）/ **私人订阅标签面板** / 全局提示词（473字 + 预设选择器 + 文本域）/ 规则区（新增规则 + 每条卡片：名称·启用·删除·条件·动作）/ 保存与重新处理 / 导出·导入·迁移预览 / 预览（样例选择 + 匹配 + 策略 + 试验面板）/ 运行设置（启用·范围·起始时间·时区·每日时间点·轮询·提前准备·发布·立即运行·历史恢复）。

**③ 标签：用户判断正确。**

- 规则**条件**里已有「订阅标签」字段（`processing-condition-editor.tsx:20`），是**多选已有标签**、不能新建（`:257` 只从 `tags` 映射，找不到的标「不可用」）—— 这正是用户要的「规则里只按标签筛选」。
- 该移走的是同一页那块 `ProcessingTags` 面板：它的写操作是 `createTag` / `renameTag` / `deleteTag` + `bindTags(selected, [tagId], "add"|"remove")`，即**标签增删改 + 按分类/列表批量为订阅源打标签**，属数据管理、不属规则。
- 注意：**建/改名/删标签目前只有这一处入口**（设置→订阅源只有批量加/减，编辑订阅弹窗只有单源），所以移走必须同时给管理功能安排新家（例如并入「设置→订阅源」，那里已有「我的标签」列）。

**两条可复用的服务端口径（digging 出来的，以后别再重新查）**

- **线上跑的是「已发布版本」，不是草稿**：`processing-worker.ts:129` 用 `store.automation.release(version)`。**改草稿不影响正在跑的规则，必须重新发布才生效**。
- **重复/重叠的聚合规则不会重复建 Story**：按 order 升序 + `claimed` 集合，先认领者独占（见上）。

`automation_draft` 是单行表（`id/revision/body`，body 是全量 JSON：`formatVersion/ownerId/global{markdown,version}/rules`），规则集就在这里，不在别的表。

## 把「每次改动跑全仓质量门」改成增量门（18:25–18:45）

用户诉求：不需要每次改代码都跑全仓门。改动落点：**AGENTS.md 的 Quality gates 章节（第 39–59 行）**——全仓规则只写在这一处，`.cursor`/`.codex`/`.agents` 里都没有重复。

新增 `scripts/check-changed.mjs` + 根 `check:changed` 脚本：只 lint 改动文件、只对「拥有改动文件的包 + 其下游」跑 `typecheck test`（`CI=1`）。

查证结论（都是实跑验证，不是推断）：

- `turbo run lint/format:check` 只有根任务 `//#lint`、`//#format:check` → 整仓，`--filter` 无效；per-file lint/format 已由 pre-commit 的 lint-staged 兜底。
- **turbo 2.10.4 的 `--filter="[HEAD]"` 在本仓非确定**：同一工作区状态连跑 3 次，归属在 `@follow/web` ↔ `Folo` 之间随机跳（`apps/*` 与 `apps/desktop/layer/*` 重叠）。归到 `Folo` 时该包没有 typecheck/test 脚本 → 任务 `cmd=<NONEXISTENT>` 被跳过、**exit 0**（静默空跑）。这是必须自己算归属的原因。
- 未跟踪新文件算改动（用 `packages/internal/logger` 放探针文件验证过）。
- `...[<ref>]` 前缀语义 = 改动包 + 依赖它的下游（用 `...@follow/logger` 验证：logger + @follow/web）。
- `typecheck` 依赖 `@follow/electron-main#build`（tsc，约 15s，有缓存）。

两处环境故障（未解决，已告知用户）：

1. 系统数据卷 `/System/Volumes/Data` 基本写满（可用曾低至 398Mi/470Mi），`npx` 出现 ENOSPC。
2. **AGENTS.md 写不进去**：Edit / rm / cp 全被文件代理拒绝（`modify backup failed`），同仓库 package.json、新建文件都正常 → 该文件被单独保护。补丁已留 `.workbuddy/AGENTS.md.patched`，用户可用 `cp .workbuddy/AGENTS.md.patched AGENTS.md` 落盘。

## 增量门首次真跑：抓到并修掉一个真类型错（18:45–19:00）

**首次完整跑 `node scripts/check:changed`（4m27s）直接失败 —— 门是有效的。**

- `@follow/web#typecheck` 报 `feeds-processing-tags.test.tsx(126,46) TS2322`：`vi.fn(async () => {})` 推断成 `Mock<Procedure | Constructable>`，与组件 prop `reload: (signal: AbortSignal) => Promise<void>` 不兼容。
- 修法：`let reload: ReturnType<typeof vi.fn<(signal: AbortSignal) => Promise<void>>>` + `vi.fn<(signal: AbortSignal) => Promise<void>>(async () => {})`。裸 `vi.fn()` 在这个 vitest 版本上没有参数推断能力，必须显式给签名。
- 注意 turbo 默认 `continue: false`：typecheck 一挂，同批的 `test` 会被**中途掐掉**，日志尾部看起来「测试全绿」其实没跑完，别据此判断测试通过。
- 复跑结果：eslint 14 文件（1 条既存 warning：feeds.tsx:650 TanStack `useVirtualizer` incompatible-library，与本轮无关）；`@follow/web` test 78 文件 / 313 用例全过、Type Errors no errors；`[check:changed] passed`，exit 0。

**单独复现某个包的检查（比整门快很多）**

- 类型：`cd apps/desktop/layer/renderer && ../../../../node_modules/.bin/tsc --noEmit`
- 单文件测试：`cd apps/desktop/layer/renderer && CI=1 ../../../../node_modules/.bin/vitest run <file>`
- `@follow/web` 的 `test` 脚本是 `vitest --typecheck`，会**同时**跑 test-d.ts 的类型断言。

**AGENTS.md 第二次复测仍是拒绝**，这次拿到更准确的报错：`ModifyBackup failed ... path index collision: suffix=09a26035.AGENTS.md, existing=, current=/Volumes/SSD/Dev/Code/Folo/AGENTS.md`（Edit 走 `ModifyBackup`，Bash `cp` 走 `Brokered file token refused`）。即文件代理侧的备份索引里有一条空路径记录与 AGENTS.md 撞车 —— 这是 harness 内部状态，不要改 `~/.workbuddy/file-history`；仍由用户在普通终端 `cp .workbuddy/AGENTS.md.patched AGENTS.md`。

**工作区未提交内容**：`feeds-processing-tags.tsx` + `feeds.tsx` + locales 把标签的建/改名/删从「处理规则页」搬到「设置→订阅源」，删掉 `action/processing-tags.tsx`；无残留引用（其余命中的是仍在的 `processing-tags-utils`）。加上 `scripts/check-changed.mjs` + `package.json` 的 `check:changed`。已过增量门，尚未跑全量门、尚未提交。

## 把标签从「处理规则」里移走：落地 + 全量门 + 真机复验（19:50–20:45）

用户指令：「把标签从规则里移走 暂时不需要单独的标签页」，外加追问「为什么要重新发布才生效 添加删除规则还需要改代码才生效吗」。
答复要点：**不需要改代码**。草稿写在 `automation_draft` 单行表，线上跑的是 `rule_set_releases` 里冻结的版本，
所以「改 → 保存并启用」就行（`save()` → `releaseRuleSet(scope,{skipDirtyCheck:true})` → `saveSchedule()` 三步一键）。

**改动落点**（已过全量门 35/35，已部署 `main-CBcdzO4Y.js`，已真机复验 12 条判据全绿）

- 新 `modules/settings/tabs/feeds-processing-tags.tsx`：`ProcessingTagManager`，默认折叠的「管理标签（n）」，建/改名/删（删除走 `ask({variant:"danger"})`）；写入后重读服务端快照，不做乐观更新。
- `settings/tabs/feeds.tsx`：挂载 manager；新增归零 effect —— 标签被删后顶部筛选器与批量目标不会停在失效 id 上。
- `processing-setting.tsx`：删掉 `<ProcessingTags />` 与 import（规则页对标签只剩规则条件里的只读筛选）。
- 删 `action/processing-tags.tsx`；`processing-tags-utils.ts` 删 `buildProcessingTagSelectionScopes`（保留 `processingFeedSourceKey` / `processingTagNames` / `filterFeedIdsByProcessingTag`）。
- i18n：`locales/app` 删 16 个只服务旧面板的 key（en/ja/zh-CN），`locales/settings` 加 11 个（同样三语，缺 key 会回落 en）。
- 测试：`processing-tags-utils.test.ts` 换成 3 项（含 `all` 筛选与失效 tag id 边界）；新增 `feeds-processing-tags.test.tsx` 8 项。

**两条新的真机/环境硬结论（都已写进 docs §15.6）**

1. **不能直接 `goto /settings/feeds`** —— 直连该路由页面是空白的（body 长 0、无 data-testid）。证据：上一轮探针日志 `tags-entry.log` 里同样直连时「我的标签」9 次探测全 0 命中，而同日志里 `/action?scope=processing_service` 8 s 就渲染出来了。必须走 `/` → `profile-menu-trigger` → `profile-menu-preferences` → `settings-tab-feeds`。
2. **本轮 `local.folo.is` 整条链路是断的**：80 端口的 Docker Apache 不通（curl 返回 000，lsof 见 `127.0.0.1:80->…` CLOSE_WAIT），且 Chromium 自己走不通外网（`api.folo.is/*` 全 `net::ERR_FAILED` → SPA 停在空白引导态）。修法是自建链式透明代理 `dev-proxy.cjs`（`local.folo.is` → 2240 保住 hostname，其余 → `$HTTP_PROXY`），Chromium 侧 `proxy:{server,bypass:"<-loopback>"}`（`<-loopback>` 必须，否则回环域名被隐式绕过）。`--host-resolver-rules=MAP local.folo.is:80 127.0.0.1:2240` 在这台机器上**不生效**。

**一条测量类教训（值得复用）**
这块标签管理曾被兄弟节点（`min-w-[1000px]` 的订阅源表格）撑到 1000px，而设置弹窗可视区只有 774px（x 501–1275），按钮落到 x≈1468 且**在横向滚动容器之外**，人点不到。
探针没抓到它，因为**探针在 `page.evaluate` 里用 `el.click()` 点**，这条路不要求元素可见 —— 「点得到」≠「看得见」。是截图暴露的。
修法是 `w-fit max-w-full` + 输入框 `w-56`；判据补强为「控件中心 `elementFromPoint` 必须命中自身且在视口内」。**这类缺陷只有几何判据能抓。**

**未处理**：那两条同名规则（`dd6466e1…` / `ba98baf6…`）仍在已发布版本里，功能无害但需用户确认后才删；工作区里另有一批不是本轮产出的未提交内容（`AGENTS.md`、`package.json`、`scripts/check-changed.mjs`、`.workbuddy/proposals/`），没动。
