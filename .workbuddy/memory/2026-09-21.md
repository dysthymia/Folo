# 2026-09-21

## Folo：第二批未提交改动按能力拆成 14 个语义化 commit

分支 `codex/web-actions-ai-v3`，基准 `77e2e5d76`（上一轮已推送到 origin）。

### 结果

14 个提交全部落地，`77e2e5d76..HEAD` 共 **101 文件 / +7063 / -561**：

```
758877c46 docs(information): 补充使用与运维手册并更新台账
75f0b9c6a fix(ai-chat): 放宽本地设置请求的 fetcher 类型
47a4d6efb feat(information-service): Story 纠错保留独立项并可追溯
5dd748f6b feat(information): 条目处理解释与来源规则入口
3720f319f feat(information): 阅读状态与快照口径
49a3d03ca feat(information): 智能／原始阅读视图切换
354f1c2b8 feat(information-service): 显式模型试运行
6eb77c811 feat(actions): 草稿导出控件与发布前身份校验
d6dcaf2d1 feat(actions): 旧规则迁移对照与本地规则停用开关
673e5bcbd feat(actions): 发布影响计数、只读历史版本与草稿回退
7df1f721e feat(actions): 订阅标签列、筛选与 List 辅助选源
d113f1b9e feat(information-service): 批处理执行链、证据编号 schema 与显示指令
3b25c3129 feat(information-core): 预设版本追踪、升级对照与 Topic 入口
6df83249f chore(i18n): 补充信息工作台与处理编辑器的第二轮文案
```

### 方法：按能力组增量物化（hunk 级拆分）

改动是「2026-09-20 产品收尾」一整轮，多个能力交织在同一文件里
（`processing-setting.tsx` 26 个 hunk 分属 6 个能力），按文件分组做不到。
用 `/tmp/folo-split/split3.py`：以 BASE 解析 `git diff -U0`，每个 hunk 标一个能力组，
第 k 组提交时把「组序 <= k」的 hunk 应用到 BASE 版本再暂存；跑完与原始 diff 逐字节校验。

这套做法已固化进 skill `semantic-commit-split`（见 §5 与 `scripts/split-capabilities.py`）。

### 本轮踩到的三个坑

1. **`-U0` 纯插入 hunk 的插入点**：`old_count == 0` 时 0 基下标是 `old_start`（不是 `old_start - 1`）。
   写错会报 65 处 MISMATCH。
2. **跨能力组的变量依赖**：`processing-setting.tsx` 的 `targetRuleId` 声明在迁移开关那一组，
   但只在后面的「解释入口」组被使用 → 中间 commit 触发 eslint `assigned a value but never used`。
   用 `line_override` 把该行挪到使用方组解决。
3. **`stage` 会把中间态写进工作区**：第 6 组被钩子拒绝后，4 个文件被留在半成品状态，
   而 `git status` 看不出异常。靠开工前的 tar 快照做独立比对才区分出「是工作区被改了，
   不是重建有 bug」。

另外：无交互 shell 里钩子找不到 `pnpm`，系列脚本需 `export PATH="/opt/homebrew/bin:$PATH"`；
`dedupe:locales` 会给 locale 排序，提交后工作区键序与 HEAD 不一致，已证明内容 deep-equal 后
`git checkout -- locales/` 对齐（排序形式是仓库约定）。

### 校验

- `materialize` 重建结果 vs 独立 tar 快照：77 个已跟踪文件 0 差异
- HEAD 内容 vs 原始改动：101 个文件，仅 6 个 locale 文件键序不同（内容等价）
- typecheck 通过：`@follow/information-service`、`@follow/information-core`、`@follow/store`、`apps/desktop/layer/renderer`
- 工作区干净，仅剩 `.pnpm-store/`、`.workbuddy/` 两个未跟踪目录

### 收尾（同日）

按用户要求补了两个 housekeeping 提交后推送：

- `1aede0b9d chore(repo): 忽略 pnpm 本地 store 目录` —— 根目录 `.pnpm-store/` 只有 8KB
  的 pnpm v11 内容寻址存储残留（不是本仓库实际用的 store），加进 `.gitignore`，文件保留不删。
- `bccffc9dc chore(workbuddy): 纳入本地工作记忆` —— 把 `.workbuddy/memory/*.md` 纳入版本库，
  提交前扫描过无密钥。钩子（prettier）未改写这两份 md。

推送结果：`77e2e5d76..bccffc9dc  codex/web-actions-ai-v3 -> codex/web-actions-ai-v3`（快进）。

### 最终状态

- 已推送并与 `origin/codex/web-actions-ai-v3` 同步
- 远端仓库：`github.com/dysthymia/Folo.git`
- 分支最新提交：`git log --oneline -1`（功能批推送点为 `bccffc9dc`，其后依次是
  gitignore、workbuddy 记忆的 housekeeping 提交）
- 工作区干净，无 stash 残留
